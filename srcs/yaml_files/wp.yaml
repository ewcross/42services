apiVersion: apps/v1
kind: Deployment
metadata:
  name: wp
  labels:
    app: wp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wp
  template:
    metadata:
      labels:
        app: wp
    spec:
      volumes:
      - name: wp-storage
        persistentVolumeClaim:
          claimName: wp-pvc
      containers:
      - name: wp
        image: wp:v1
        imagePullPolicy: Never
        ports:
        - containerPort: 80
          name: wp
        env:
        - name: WP_DB_NAME
          value: wordpress_db
        - name: WP_DB_USER
          value: wordpress_user
        - name: WP_DB_PASSWORD
          value: wordpress
        - name: WP_DB_HOST
          value: mysql
        volumeMounts:
        - name: wp-storage
          mountPath: /wp_files
---
apiVersion: v1
kind: Service
metadata:
  name: wp
  labels:
    app: wp
  annotations:
    metallb.universe.tf/allow-shared-ip: abcd
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.64.22  
  selector:
    app: wp
  ports:
    - name: wp
      protocol: TCP
      port: 5050
      targetPort: 80
