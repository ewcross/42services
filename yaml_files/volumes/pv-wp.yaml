apiVersion: v1
kind: PersistentVolume
metadata:
  name: wp-pv
  labels:
    app: wp
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 200Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "./storage"
